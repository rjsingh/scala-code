package funsets

object Main extends App {
  import FunSets._
 // println(contains(singletonSet(1), 1))

  def x = (x: Int) => {List(1,3,4,5,7,1000) contains x}
  def y = (y: Int) => {List(1,2,3,4) contains y}
  
  def z = diff(x, y)
  
  println(z(2))
}
